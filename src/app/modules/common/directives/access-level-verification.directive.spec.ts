import { AccessLevelVerificationDirective } from './access-level-verification.directive';

describe('AccessLevelVerificationDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessLevelVerificationDirective();
    expect(directive).toBeTruthy();
  });
});
